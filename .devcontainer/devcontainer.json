{
    "image": "mcr.microsoft.com/devcontainers/typescript-node",
    "customizations": {
        "vscode": {
            "extensions": ["streetsidesoftware.code-spell-checker"]
        }
    },
    "forwardPorts": [3005],
    // "postCreateCommand": "sudo apt update && sudo apt install vim -y && touch hello.txt && echo Hello > hello.txt"
    "postCreateCommand": "bash scripts/echohello.sh",
    "postStartCommand": "node index.js"
}